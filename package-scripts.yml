scripts:
  start: &serve
    default: 'webpack-serve --config webpack.dev.config'

  build:
    <<: *serve  # alias build.default to start.default
    development: 'webpack --config webpack.dev.config'
    production: 'webpack --config webpack.prod.config'

  test:
    default: 'cross-env
      TS_NODE_PROJECT=test/tsconfig.json
      mocha
    '
    coverage: 'cross-env
      TS_NODE_PROJECT=test/tsconfig.json
      nyc mocha
    '
